/* Podemos fazer função atribuindo variáveis */

let minhaFuncao = function(a, b, c) {
    console.log(`Valor de "A": ${a}`)
    console.log(`Valor de "B": ${b}`)
    console.log(`Valor de "C": ${c}`)

    return a + b + c
}

minhaFuncao(5, 7, 9)